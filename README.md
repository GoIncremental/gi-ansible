Prereqiusites:

You need to specify the following environment variables:

SSL_CERT_FILE=/usr/local/opt/curl-ca-bundle/share/ca-bundle.crt
DO_CLIENT_ID=[your digital ocean client id]
DO_API_KEY=[your digital ocean api key]

You also need to have the digital ocean box available to vagrant